services:
  webdav-mgmt:
    image: node:24-alpine
    container_name: webdav-mgmt-app
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8080:5173"
    command: sh -c "npm install && npm run dev -- --host"
    restart: unless-stopped
